import { nord } from "cm6-theme-nord";

export enum Themes {
  Nord,
  BasicDark,
  BasicLight,
  MaterialDark,
  Default,
}

export enum Languages {
  Cpp,
  Java,
  JavaScript,
  Python,
}

export type Extenstion = typeof nord;
